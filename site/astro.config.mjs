// @ts-check
import { defineConfig } from 'astro/config';
import react from '@astrojs/react';

// Legis member site — its own subdomain root (IA §1.3, §1.4):
//   site:  https://legis.foundryside.dev
//   base:  '/'   (every member site is a domain root, no subpath)
// Cross-subdomain links to siblings are ABSOLUTE https://{member}.foundryside.dev
// URLs (generated by the shared @weft/site-kit data), so no base-path gymnastics.
export default defineConfig({
  site: 'https://legis.foundryside.dev',
  base: '/',
  integrations: [react()],
});
